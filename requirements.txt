# Core data + backtesting
numpy>=1.26
pandas>=2.1
yfinance>=0.2.40

# Dashboard
streamlit>=1.36
plotly>=5.22

# Results charts + equity curves (plot_results.py, plot_equity.py)
matplotlib>=3.8

# Bot 2 — news/sentiment scoring (news_sentiment.py)
vaderSentiment>=3.3

# Encrypted trade journal synced to Google Drive
cryptography>=42.0
google-auth>=2.30
google-auth-oauthlib>=1.2
google-api-python-client>=2.130

# Live execution via Charles Schwab (only needed when DRY_RUN=False)
schwab-py>=1.3
